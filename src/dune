(library
  (name react)
  (public_name react)
  (modules react))
(executable
  (name react_top)
  (modules react_top react_top_init)
  (libraries  compiler-libs.toplevel react)
  (link_flags (-linkall))
  (modes byte))
(env
  (dev
    (flags (:standard -warn-error -A))))